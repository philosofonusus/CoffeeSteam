<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-Y590WDGQPK"></script>
    <script>
     window.dataLayer = window.dataLayer || [];
     function gtag(){dataLayer.push(arguments);}
     gtag('js', new Date());

     gtag('config', 'G-Y590WDGQPK');
    </script>

    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" media="print" onload="this.media='all'" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;500;600;700&display=swap" media="print" onload="this.media='all'" rel="stylesheet">
    <noscript>
        <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" type="text/css" media="print"
        onload="this.media='all'" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;500;600;700&display=swap" type="text/css" rel="stylesheet">
    </noscript>
    <meta name="description" content="{{ description }}">
    <script src="https://cdn.jsdelivr.net/npm/typed.js@2.0.12"></script>
    <script src="//code-eu1.jivosite.com/widget/Mq4rW0wzUi" defer></script>
</head>
<body>
    {{ content | safe }}
    {% include 'footer.njk' %}
    <!-- Start FastcallAgent code {literal} -->
        <script type="text/javascript">
            var fca_code = '84a012aa3e7615af593c137d817f7758';
            (function() {
                var po = document.createElement('script'); po.type = 'text/javascript'; po.async = true; po.charset = 'utf-8';
                po.src = '//ua.cdn.fastcallagent.com/fca.min.js?_='+Date.now();
                var s = document.getElementsByTagName('script')[0];
                if (s) { s.parentNode.insertBefore(po, s); }
                else { s = document.getElementsByTagName('head')[0]; s.appendChild(po); }
            })();
        </script>
    <!-- {/literal} End FastcallAgent code -->

    <script>
        window.addEventListener("load", startup, false);
        let typedonce = false;
        
const addOnScreen = (entries, observer) => {
  entries.forEach(entry => {
    !entry.target.classList.contains("wasonscreen") && entry.isIntersecting && entry.target.classList.add("wasonscreen");

    if(entry.target.classList.contains("wasonscreen") && !typedonce && entry.target.classList.contains('typing')){

        const options = {
            strings: ['{{description}}'],
            typeSpeed: 30
        };
        typedonce = true
        const typed = new Typed('.typing', options);
    }

    entry.target.classList.toggle("onscreen", entry.isIntersecting);
  });
};
function startup(){ 
console.log(1)
    const observer = new IntersectionObserver(addOnScreen);
const options = { root: null, rootMargin: '0px', threshold: 0.5 }; 

const elements = document.querySelectorAll('*');
elements.forEach(el => {
  observer.observe(el, options);
});
}   
    const descriptionExpandHandler = (id) => {
        document.getElementById(`more${id}`).classList.toggle("active");
    }

    const moreButton1 = document.getElementById('more-button1').addEventListener('click', () => descriptionExpandHandler(1))
    const moreButton2 = document.getElementById('more-button2').addEventListener('click', () => descriptionExpandHandler(2))
    const moreButton3 = document.getElementById('more-button3').addEventListener('click', () => descriptionExpandHandler(3))

    
    </script>
</body>
</html>